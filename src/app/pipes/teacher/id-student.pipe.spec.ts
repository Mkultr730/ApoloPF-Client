import { IdStudentPipe } from './id-student.pipe';

describe('IdStudentPipe', () => {
  it('create an instance', () => {
    const pipe = new IdStudentPipe();
    expect(pipe).toBeTruthy();
  });
});
